* text=auto eol=lf

# Shell scripts must keep LF endings — they run inside Linux containers.
*.sh text eol=lf
docker-entrypoint.sh text eol=lf

# SQL migrations: LF for portability.
*.sql text eol=lf
